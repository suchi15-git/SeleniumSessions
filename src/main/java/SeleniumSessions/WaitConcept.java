package SeleniumSessions;

public class WaitConcept {

	public static void main(String[] args) {
	//wy do we need wait - what if the application is taking time or slow, elements are visible it will throw exception
		//to avoid such problems we need to add selenium wait
		//sync between script and application
		
		//static wait (thread.sleep)
		//dynamic wait

	}

}
